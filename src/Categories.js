import React from "react";

const Categories = () => {
	return (
		<div>
			<button>shakes</button>
			<button>breakfast</button>
			<button>lunch</button>
		</div>
	);
};

export default Categories;
